done:
- template files
- templates
  - named templates
  - anonymous templates
- template rules
  - constant templates
- template calls
  - direct
- comments
- property access
  - direct
- dynamic scoping
- objectoriented functions
  - generic functions
    - empty
    - trim = remove leading/trailing whitespaces from string
    - compress = replace whitespaces in string with space 
  - list functions
    - first/rest = select first element/all without first element
    - last/trunc = select last element/all without last element
    - element(i) = select element with index i
    - len = length
    - strip = remove nulls from list
  - map functions
    - keys
    - values
  - object functions
    - properties
    - type
  - custom functions
- control structures
  - if-template
  - for-template
- element iterators
  - iteration index
- import strategy
- variable existence checks
- list & map concatenation
- reflective references
  - to templates
- global data
- strongly typed arguments, typed expressions are wrapped with cast

next:
- error reporting
- reflective references
  - to variables
- comtemplate unit
- template coverage
- inline imports
